# Model Checkpoint Layout

Place local model checkpoints here only for local reproduction. Do not commit
large model files to GitHub.

Expected files after downloading the external asset package:

```text
models/model.safetensors
models/dinov2_vit_base_patch14_lvd142m.safetensors
```

The checkpoints and checksums are described in `../WEIGHTS_AND_CHECKPOINTS.md`.
