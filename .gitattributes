# This lightweight GitHub archive intentionally does not track image datasets
# or model checkpoints. If large assets are later committed through a separate
# Git LFS branch/release workflow, uncomment and use appropriate LFS rules.
#
# *.safetensors filter=lfs diff=lfs merge=lfs -text
# *.jpg filter=lfs diff=lfs merge=lfs -text
# *.jpeg filter=lfs diff=lfs merge=lfs -text
# *.png filter=lfs diff=lfs merge=lfs -text
# *.webp filter=lfs diff=lfs merge=lfs -text
# *.bmp filter=lfs diff=lfs merge=lfs -text
